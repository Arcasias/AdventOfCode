Bun.file("2023/01/input.txt")
  .text()
  .then((input) =>
    console.log(
      -input
        .replace(/[^\d\n]/g, "")
        .split("\n")
        .reduce((acc, line) => (acc -= "" + line[0] + line.at(-1)), 0)
    )
  );
